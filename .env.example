# This file serves as an example for the required environment variables.
# Copy this file to .env and fill in the actual values.

# --- OpenAI / Avalai API Credentials ---
# Your API key for the embedding service (e.g., OpenAI, Avalai).
OPENAI_API_KEY="YOUR_AVALAI_API_KEY_HERE"

# The base URL for the OpenAI-compatible API.
# For Avalai, this is the correct URL. For OpenAI, it would be https://api.openai.com/v1
OPENAI_BASE_URL="https://api.avalai.ir/v1"

# --- Qdrant Vector Database ---
# The URL of your Qdrant instance.
QDRANT_URL="http://localhost:6333"

# The name of the collection to store the vectors in.
QDRANT_COLLECTION_NAME="pdf_documents"

# --- MinIO Object Storage ---
# The root user for your MinIO instance.
MINIO_ROOT_USER="minioadmin"

# The root password for your MinIO instance.
MINIO_ROOT_PASSWORD="minioadmin"

# The hostname for your MinIO instance.
MINIO_HOST="localhost"

# The port for your MinIO instance.
MINIO_PORT="9000"

# The name of the bucket to store uploaded PDFs in.
MINIO_BUCKET="docs"

# --- PDF Processing ---
# The language to use for OCR. "fas" is for Persian (Farsi).
OCR_LANGUAGE="fas"
